import UIKit

// Tab bar visibility is controlled by SwiftUI: only the five tab roots (Home, Discover, Sell, Inbox, Profile)
// show the tab bar; all pushed views use .toolbar(.hidden, for: .tabBar). No custom UIKit manipulation.
